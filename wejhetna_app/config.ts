
export const API_BASE_URL = "https://swampland-geologic-femur.ngrok-free.dev";
export const NEARBY_DRIVER_RADIUS_M = 3000;
export const RIDE_STATUS_POLL_INTERVAL_MS = 6000;

/** Bump when ride UI/API behavior changes — shown on map modal + driver rides so you know the new bundle is installed. */
export const RIDE_UI_BUILD = "c10-driver-marker";
