# בדיקת וולידציה של Boundaries

## מה נבדק:
1. ✅ API endpoint `/cities/check-boundary` עובד
2. ✅ Frontend קורא ל-API נכון
3. ✅ וולידציה ב-`handleMapPress` - חוסמת מיקומים מחוץ ל-3 הערים
4. ✅ וולידציה ב-`handleUseMyLocation` - חוסמת מיקומים מחוץ ל-3 הערים
5. ✅ וולידציה ב-`handleConfirm` - בדיקה אחרונה לפני מעבר לטופס

## נקודות לבדיקה ידנית:

### 1. בדיקת מיקום בתוך אחת מ-3 הערים (צריך לעבור):
- **תל שבע**: 31.243, 34.843
- **רהט**: 31.395, 34.755
- **לקיה**: 31.319, 34.888

### 2. בדיקת מיקום מחוץ ל-3 הערים (צריך להיחסם):
- **באר שבע**: 31.243, 34.792 (מחוץ לתל שבע)
- **ירושלים**: 31.768, 35.214 (רחוק מאוד)
- **תל אביב**: 32.085, 34.782 (רחוק מאוד)

## איך לבדוק:

1. **הפעל את האפליקציה** (React Native)
2. **התחבר כ-Admin**
3. **עבור להוספת מקום חדש** → בחר מיקום על המפה
4. **נסה מיקומים שונים:**
   - מיקום בתוך אחת מ-3 הערים → צריך לעבור
   - מיקום מחוץ ל-3 הערים → צריך להופיע Alert עם הודעת שגיאה

## בדיקות אוטומטיות (אופציונלי):

אפשר ליצור test script שיבדוק את ה-API endpoint ישירות.

